/**
* main - entry point of the function
* Return: 0 if successful
*/

#include<stdio.h>

int main(void)
{
int i, j;
int base;
base = 10;
for (i = 0; i < base; i++)
{
for (j = 0; j < base; j++)
{
  if (i != j){
    
putchar('0' + i);
 putchar('0' + j);
  };
};
 };
putchar('\n');
return (0);
}
